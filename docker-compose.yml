services:
  azuracast-player:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production
    command: ["npx", "live-server", "--host=0.0.0.0", "--port=8080", "--open=."]